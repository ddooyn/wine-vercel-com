import type { NextPage } from "next";
// import가 뭔가요? > 지금은 몰라도 됨.
// NextPage는 TypeScript에서 타입 선언.

const WinePage: NextPage = () => {
    return (
        <div>
            <h1>Wine</h1>
        </div>
    )
}

export default WinePage;
// export가 뭔가요? > 지금은 몰라도 됨.
// 가장 기본적인 템플릿, TypeScript를 쓰는